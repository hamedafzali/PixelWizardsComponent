version: "3.9"

services:
  pixelwizards:
    build:
      context: .
    ports:
      - "8000:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
