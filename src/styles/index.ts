import "./global.css";
import "./components.css";

// Export style utilities
export const injectGlobalStyles = () => {
  // This function can be called to ensure global styles are loaded
  // In a real implementation, you might use a CSS-in-JS solution
  // or ensure the CSS is imported in your application's entry point
};

export default injectGlobalStyles;
